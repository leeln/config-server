server:
  port: ${PORT:8083}

management:
  context-path: /admin

spring:
  cloud:
    config:
      server:
        git:
          uri: git@gitlab.com:micoa/config-repo
          basedir: build/config
#eureka:
#  instance:
#    hostname: ${HOSTNAME:localhost}
#    nonSecurePort: ${server.port}
#  client:
#    serviceUrl:
#      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}

#zuul:
#  routes:
#    user-center: /orders/**
#    order-center: /users/**


spring:
  application:
    name: config-server

eureka:
  instance:
    hostname: localhost
    nonSecurePort: 8080
  client:
    serviceUrl:
      defaultZone: http://eureka.leeln.com/eureka/
    fetch-registry: false
    register-with-eureka: true
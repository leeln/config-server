spring:
  application:
    name: config-server

eureka:
  instance:
    hostname: eureka.leeln.com
    nonSecurePort: 80
  client:
    serviceUrl:
      defaultZone: http://eureka.leeln.com/eureka/
    fetchRegistry: false
    registerWithEureka: true